# Система документов и экспорта

## Компоненты

### 1. DocumentPreviewModal
Универсальный модал для предпросмотра и экспорта документов

**Возможности:**
- ✅ Предпросмотр в формате A4 (210×297 мм)
- ✅ Печать через браузер (`window.print()`)
- ✅ Экспорт в PDF (через backend)
- ✅ Экспорт в Excel (через backend)
- ✅ Экспорт в Word (через backend)
- ✅ Отправка по email
- ✅ Сохранение в базу данных
- ✅ Автоматическая разметка для печати

**Использование:**
```tsx
import DocumentPreviewModal from '@/components/DocumentPreviewModal';
import TTNTemplate from '@/components/templates/TTNTemplate';

<DocumentPreviewModal
  isOpen={showModal}
  onClose={() => setShowModal(false)}
  documentTitle="Товарно-транспортная накладная"
  documentType="ttn"
>
  <TTNTemplate data={documentData} />
</DocumentPreviewModal>
```

### 2. TTNTemplate
Шаблон товарно-транспортной накладной

**Формат:** A4 с полями 20мм  
**Данные:**
- Номер и дата заказа
- Грузоотправитель/получатель
- Характеристика груза
- Транспорт и водитель
- Подписи сторон

## Создание новых шаблонов

### Структура шаблона

```tsx
interface MyDocumentTemplateProps {
  data: {
    // Ваши поля данных
  };
}

export default function MyDocumentTemplate({ data }: MyDocumentTemplateProps) {
  return (
    <div className="font-sans text-sm">
      {/* HTML разметка с Tailwind CSS */}
    </div>
  );
}
```

### Рекомендации по вёрстке

1. **Размеры:**
   - Ширина: 210mm (формат A4)
   - Высота: минимум 297mm
   - Поля: 20mm

2. **Стили для печати:**
   ```css
   @media print {
     @page {
       size: A4;
       margin: 20mm;
     }
   }
   ```

3. **Классы Tailwind:**
   - Используйте `text-sm`, `text-xs` для текста
   - Таблицы: `border-collapse`, `border border-gray-400`
   - Заголовки: `text-lg`, `font-bold`
   - Подписи: `border-b border-gray-400` для линий

4. **Разрывы страниц:**
   ```css
   .page-break {
     page-break-after: always;
   }
   ```

## Типы экспорта

### PDF (рекомендуется)
- ✅ Лучший формат для печати и архивирования
- ✅ Сохраняет форматирование 1:1
- ✅ Не редактируется (защита документа)
- 📄 Backend: Python + weasyprint

### Excel
- ✅ Для редактирования табличных данных
- ✅ Можно использовать формулы
- ⚠️ Теряется часть визуального форматирования
- 📊 Backend: Python + openpyxl

### Word
- ✅ Для редактирования текста
- ✅ Можно добавлять комментарии
- ⚠️ Может измениться верстка
- 📝 Backend: Python + python-docx

## Backend функции (TODO)

### 1. /backend/export-pdf/
```python
# HTML → PDF конвертация
from weasyprint import HTML

def handler(event, context):
    html_content = event['body']['html']
    pdf = HTML(string=html_content).write_pdf()
    return {'statusCode': 200, 'body': base64_pdf}
```

### 2. /backend/export-excel/
```python
# Данные → Excel таблица
from openpyxl import Workbook

def handler(event, context):
    data = event['body']['data']
    wb = Workbook()
    # ... заполнение данными
    return {'statusCode': 200, 'body': base64_excel}
```

### 3. /backend/send-email/
```python
# Отправка документа по email
import smtplib

def handler(event, context):
    email = event['body']['email']
    pdf_data = event['body']['pdf']
    # ... отправка email
    return {'statusCode': 200}
```

## Примеры документов

### Договор-Заявка
- Реквизиты сторон
- Условия перевозки
- Маршруты и сроки
- Стоимость услуг
- Подписи и печати

### УПД (Универсальный передаточный документ)
- Данные продавца/покупателя
- Перечень товаров/услуг
- Суммы с НДС
- Подписи уполномоченных лиц

### Акт выполненных работ
- Реквизиты заказчика/исполнителя
- Перечень работ
- Стоимость
- Подтверждение выполнения

## Интеграция с заказами

```tsx
// Получить данные из заказа
const orderData = getOrderById(orderId);

// Преобразовать в формат документа
const ttnData = {
  orderNumber: orderData.orderZone + '-' + orderData.orderNumber,
  orderDate: orderData.orderDate,
  from: orderData.routes[0].from,
  to: orderData.routes[0].to,
  // ...
};

// Показать предпросмотр
<DocumentPreviewModal>
  <TTNTemplate data={ttnData} />
</DocumentPreviewModal>
```

## Roadmap

- [ ] Backend функции экспорта (PDF, Excel, Word)
- [ ] Шаблон Договор-Заявка
- [ ] Шаблон УПД
- [ ] Шаблон Акта выполненных работ
- [ ] Email интеграция
- [ ] Хранение документов в БД
- [ ] Электронная подпись (опционально)
- [ ] Штрих-коды и QR-коды
- [ ] Версионирование документов
